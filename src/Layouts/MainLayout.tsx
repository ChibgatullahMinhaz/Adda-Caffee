import React from 'react';
import Navbar from '../Shared/Navbar';

const MainLayout = () => {
    return (
        <>
                 <header>
        <nav>
          <Navbar></Navbar>
        </nav>
      </header>

        </>
    );
};

export default MainLayout;